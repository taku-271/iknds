{
  "name": "iknds",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev:frontend": "npm run -w frontend dev"
  },
  "workspaces": [
    "packages/*"
  ],
  "license": "ISC"
}
